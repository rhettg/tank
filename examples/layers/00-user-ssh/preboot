#!/usr/bin/env -S uv run --quiet --script
# /// script
# requires-python = ">=3.12"
# dependencies = ["pyyaml"]
# ///
import os
from pathlib import Path

import yaml


def get_env(name: str) -> str:
    value = os.environ.get(name)
    if not value:
        raise SystemExit(f"{name} is not set")
    return value


cloud_init_path = Path(get_env("GI_CLOUD_INIT"))
username = get_env("USER")
home = Path(get_env("HOME"))

if not cloud_init_path.exists():
    raise SystemExit(f"GI_CLOUD_INIT does not exist: {cloud_init_path}")

key_path = None
for candidate in (home / ".ssh/id_ed25519.pub", home / ".ssh/id_rsa.pub"):
    if candidate.exists():
        key_path = candidate
        break

if key_path is None:
    raise SystemExit("No SSH public key found at ~/.ssh/id_ed25519.pub or ~/.ssh/id_rsa.pub")

ssh_key = key_path.read_text().strip()

raw = cloud_init_path.read_text()
prefix = ""
if raw.startswith("#cloud-config"):
    prefix = "#cloud-config\n"
    raw = raw[len(prefix):]

data = yaml.safe_load(raw) if raw.strip() else {}
if data is None:
    data = {}

users = data.get("users")
if users is None:
    users = []
elif not isinstance(users, list):
    raise SystemExit("cloud-init 'users' must be a list")

existing = None
for user in users:
    if isinstance(user, dict) and user.get("name") == username:
        existing = user
        break

payload = {
    "name": username,
    "sudo": "ALL=(ALL) NOPASSWD:ALL",
    "shell": "/bin/bash",
    "ssh_authorized_keys": [ssh_key],
}

if existing is None:
    users.append(payload)
else:
    existing.update(payload)

data["users"] = users

serialized = yaml.safe_dump(data, sort_keys=False, default_flow_style=False)
cloud_init_path.write_text(prefix + serialized)
